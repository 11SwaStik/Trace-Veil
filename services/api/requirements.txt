# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- Database (async PostgreSQL) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# --- Config / env vars ---
python-dotenv==1.0.1

# --- Validation (used by FastAPI under the hood; pinned for reproducibility) ---
pydantic==2.10.3
email-validator==2.2.0

# --- Auth ---
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# --- Task queue (dispatching only — the worker lives in simulation_engine/) ---
celery[redis]==5.4.0

# --- Dev / test ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
