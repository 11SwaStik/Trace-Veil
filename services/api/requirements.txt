# --- Web framework ---
fastapi
uvicorn[standard]

# --- Database (async PostgreSQL) ---
sqlalchemy[asyncio]
asyncpg

# --- Config / env vars ---
python-dotenv

# --- Validation (used byFastAPI under the hood; pinned for reproducibility) ---
pydantic
email-validator

# --- Auth ---
passlib[bcrypt]
bcrypt<5.0

python-jose[cryptography]

# --- Task queue (dispatching only — the worker lives in simulation_engine/) ---
celery[redis]

# --- Dev / test ---
pytest
pytest-asyncio
httpx
aiosqlite
